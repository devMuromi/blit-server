version: "3"
services:
  django:
    build: .
    ports:
      - 80:8000
    env_file:
      - .env
    volumes:
      - ./static:/staticfiles
    command: python manage.py runsslserver --certificate django.crt --key django.key
